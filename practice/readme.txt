1. python 

2. pip - python install package
pip install python-docx
to save packages: pip freeze  > requirements.txt
to load packages: pip install -r requirements.txt

3. venv - virtual environment
to create virtualenv: virtualenv FOLDER
to start: FOLDER\Scripts\activate
to stop: deactivate

4. pyTest - python testing
open all test_*.py files and run test_* functions

5. Selenium
install selenium ide: chrome extension to record scripts
record the browser actions
export to pyTest

6. Git - Version control
save all practice in repository

Target: 
1. Prepare script (bat) that:
- Starts virtual env
- Get code from git
- Run pyTests scripts

2. Prepare more useful scripts that stimulate login, search, sending parameters, etc'
All are samples of selenium